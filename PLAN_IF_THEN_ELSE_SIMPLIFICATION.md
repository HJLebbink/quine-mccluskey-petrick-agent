# Superseded: If-Then-Else Simplification Plan

**This plan was executed.** The `src/simplify/` module and JSON API are already implemented.

The feature described in this document was implemented and is now live:
- `src/simplify/` — parser, analyzer, optimizer, deadlock detection
- `src/agent_api.rs` — JSON API for Claude integration
- `qm-agent simplify` subcommand — CLI entry point

This document is retained as historical context only. It was a pre-implementation design plan.

**Current canonical docs:**
- `README.md` — Claude Integration section
- `CLAUDE.md` — Claude Integration section
- `examples/agent/README.md` — API reference
